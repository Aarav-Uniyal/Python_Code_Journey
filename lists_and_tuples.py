grocery = ["Harpic", "vim bar", "deodrant", "Bhindi",
           "Lollypop", 56]
# print(grocery[5])
numbers = [2, 7, 9, 11, 3]
print(max(numbers))
print(min(numbers))
# numbers.remove(9)
# numbers.pop()
# numbers.sort()
# numbers = []
# numbers.reverse numbers.insert(2, 67)
# nums = numbers.copy() or n()
# # numbers.append(1)
# # numbers.append(72)
# # numbers.append(5)
# #ums = numbers[:]
# print(numbers)
# 3, 11, 9, 7, 2
# print(numbers)
# numbers[1] = 98
# print(numbers)

# Mutable - can change
# Immutable - cannot change
# tp = (1,) # We need to add a comma as the interpreter will think it
            # is an integer and not a tuple.
# print(tp)
a = 1
b = 8
a, b = b, a  # Python allows us to swap values very easily.
# temp = a
# a = b      <---- This is the traditional method of swapping values.
# b = temp
print(a, b)

